specVersion: "v1beta"
serviceId: "quicklink"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "quicklink-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
